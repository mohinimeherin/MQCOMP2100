In this page I created an HTML file that includes:

- A header introducing myself using `<h1>` and `<p>` tags.
- A section with id "units" containing a table listing my MQ unit codes.
- A section with id "images" containing two images using `<figure>` and `<figcaption>`:
  - One image stored locally in the project folder.
  - One image loaded from a remote URL.
- A section with id "video" embedding a video using the `<video>` tag from a remote source.
- A footer containing copyright information.